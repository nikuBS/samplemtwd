var skt_landing = {},
  frontend_fn = {
  },
  component_list = [],
  widget_list = [],
  resize_fn = [], //resize시 사용
  scroll_fn = [], //scroll시 사용
  wheel_fn = [] //wheel시 사용
$(document).on('ready', function () {
});
