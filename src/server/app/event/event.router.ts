import TwRouter from '../../common/route/tw.router';

class EventRouter extends TwRouter {
  constructor() {
    super();
    // this._controllers.push({ url: '/', controller: new HomeMainController() });
  }
}

export default EventRouter;

